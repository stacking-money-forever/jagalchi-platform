{
  "name": "@jagalchi/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check:contract": "node scripts/check-contract-hash.mjs",
    "check:upstream-sync": "node scripts/check-upstream-sync.mjs",
    "check:generated": "node scripts/generate.mjs --check",
    "check:pack": "node scripts/check-pack.mjs",
    "build": "pnpm check:contract && pnpm check:generated && tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "pnpm check:contract && pnpm check:generated && tsc --noEmit -p tsconfig.json",
    "typecheck:consumers": "pnpm build && pnpm check:pack && tsc --noEmit -p test/tsconfig.web.json && tsc --noEmit -p test/tsconfig.mobile.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "openapi-typescript": "^7.10.1",
    "typescript": "5.9.3",
    "vitest": "^4.1.5"
  }
}
